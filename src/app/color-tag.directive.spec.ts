import { ColorTagDirective } from './color-tag.directive';

describe('ColorTagDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorTagDirective();
    expect(directive).toBeTruthy();
  });
});
